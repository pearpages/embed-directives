(function (){
	'use strict';
	
	angular.module('demo',['ui.router','mandatory'])
	.config(function() {
		// fileConfigProvider can be used to setup the buttons, there are also other providers that can be used
	})
	
}());